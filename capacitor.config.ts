import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sysserve.app',
  appName: 'Sysserve',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
